<Window x:Class="FileOrganizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileOrganizer"
        mc:Ignorable="d"
        Title="Organizador de Arquivos"
        Width="1200"
        Height="720"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        MaxWidth="1200"
        MaxHeight="720"
        MinWidth="1200"
        MinHeight="720">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!-- Menu lateral -->
        <DockPanel Grid.Column="0" LastChildFill="True" Background="{StaticResource Primary}">
            <StackPanel DockPanel.Dock="Top" Margin="12">
                <TextBlock Text="Navegação" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="4,8,4,16"/>
                <!-- Navegação por comando nativo -->
                <Button Content="Organizar Pasta" Click="BtnOrganizar_Click"/>
                <Button Content="Renomear em Lote" Click="BtnRenomear_Click"/>
                <Button Content="Procurar Arquivos Duplicados" Click="BtnDuplicados_Click"/>
            </StackPanel>
        </DockPanel>

        <!-- Conteúdo -->
        <Frame x:Name="MainFrame"
               Grid.Column="1"
               Source="Views/OrganizarArquivosPage.xaml"
               NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
